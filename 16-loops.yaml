- name: Demo on loops
  hosts: local
  connection: local
  tasks:
  - name: print names
    debug:
      msg: "Hi {{ item }}" # item is spl reserve variable
    loop:
    - Ramesh
    - Kishore
    - venkat