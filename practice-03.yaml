- name: "Install and start nginx"
  hosts: frontend
  become: yes
  tasks:
  - name: "Installing nginx"
    ansible.builtin.package: 
      name: "nginx"
      state: present
  - name: "Start nginx"
    service:
      name: "nginx"
      state: started
      enabled: yes
